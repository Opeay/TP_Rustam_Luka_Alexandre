<form method="post" action="{{ path('app_email_adresse_delete', {'id': email_adresse.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ email_adresse.id) }}">
    <button class="btn">Delete</button>
</form>
