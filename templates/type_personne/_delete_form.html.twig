<form method="post" action="{{ path('app_type_personne_delete', {'id': type_personne.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ type_personne.id) }}">
    <button class="btn btn-danger btn-sm">Delete</button>
</form>
