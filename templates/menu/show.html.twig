{% extends 'base.html.twig' %}

{% block title %}Menu{% endblock %}

{% block body %}
<div class="center text-light w-90">
    <h1>Menu</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ menu.id }}</td>
            </tr>
            <tr>
                <th>Rang</th>
                <td>{{ menu.rang }}</td>
            </tr>
            <tr>
                <th>Libelle</th>
                <td>{{ menu.libelle }}</td>
            </tr>
            <tr>
                <th>Route</th>
                <td>{{ menu.route }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_menu_index') }}" class="btn btn-small btn-secondary">Retour à la liste</a>

    <a href="{{ path('app_menu_edit', {'id': menu.id}) }}" class="btn btn-small btn-secondary">Modifier</a>

    {{ include('menu/_delete_form.html.twig') }}
</div>
{% endblock %}
