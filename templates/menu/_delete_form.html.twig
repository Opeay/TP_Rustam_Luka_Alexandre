<form method="post" action="{{ path('app_menu_delete', {'id': menu.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer ce menu ?');">
    <input type="hidden" name="_token" value="{{ csrf_token('Supprimer' ~ menu.id) }}">
    <button class="btn btn-small btn-secondary">Delete</button>
</form>
