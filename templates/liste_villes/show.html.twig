{% extends 'base.html.twig' %}

{% block title %}ListeVilles{% endblock %}

{% block body %}
    <h1>ListeVilles</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ liste_ville.id }}</td>
            </tr>
            <tr>
                <th>Ville</th>
                <td>{{ liste_ville.Ville }}</td>
            </tr>
            <tr>
                <th>CodePostal</th>
                <td>{{ liste_ville.CodePostal }}</td>
            </tr>
            <tr>
                <th>Region</th>
                <td>{{ liste_ville.Region }}</td>
            </tr>
            <tr>
                <th>Pay</th>
                <td>{{ liste_ville.Pay }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_liste_villes_index') }}">back to list</a>

    <a href="{{ path('app_liste_villes_edit', {'id': liste_ville.id}) }}">edit</a>

    {{ include('liste_villes/_delete_form.html.twig') }}
{% endblock %}
