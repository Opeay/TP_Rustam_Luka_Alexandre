{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-md btn-primary m-4">{{ button_label|default('Sauvegarder') }}</button>
    <a href="{{path('app_cursus_index')}}" class="btn btn-md btn-primary m-4">Retour</a>
{{ form_end(form) }}
