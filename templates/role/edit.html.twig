{% extends 'base.html.twig' %}

{% block title %}Edit Role{% endblock %}

{% block body %}
<div class=" text-light w-90 align-items-center center">
    <h1>Modifier {{ role.libelle }}</h1>

        {{ include('role/_form.html.twig', {'button_label': 'Modifier'}) }}
    <div class="d-flex justify-content-around">
        <a href="{{ path('app_role_index') }}" class="btn btn-small btn-secondary">Retour à la liste</a>
        {{ include('role/_delete_form.html.twig') }}
    </div>

</div>
{% endblock %}
