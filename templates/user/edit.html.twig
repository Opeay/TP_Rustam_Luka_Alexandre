{% extends 'base.html.twig' %}

{% block title %}Edit User{% endblock %}

{% block body %}
<div class=" text-light w-90 align-items-center center">
    <h1>Modifier L'utilisateur {{ user.username }}</h1>

    {{ include('user/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_user_index') }}" class="btn btn-small btn-secondary my-3">Retour à la liste</a>

    {{ include('user/_delete_form.html.twig') }}
</div>
{% endblock %}
